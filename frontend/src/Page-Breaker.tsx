//will have search bar, home button, login butotn, and contact button

import React from "react";

// Used on the main page to divide the Home and Meet the Team sections
const Pagebreaker = () => {
  {
    return (
      <div className="pageBreak">
        <h2 className="blankheader"> </h2>
      </div>
    );
  }
};

export default Pagebreaker;
